import { ethers } from 'ethers';
import hre from 'hardhat';

export default async () => {
    // Hardhat automatically starts a network when used
    // await hre.network.provider.request({
    //     method: 'hardhat_reset',
    //     params: [],
    // });
    // hre.network.name = 'localhost';
};
